apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: torchserve
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: application
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://intel.github.io/helm-charts
    chart: torchserve
    targetRevision: 0.1.0
    helm:
      valuesObject:
        image:
          repository: pytorch/torchserve
          tag: 0.8.2

        # 📦 модель для прикладу
        modelStore:
          models:
            densenet161.mar:
              url: https://torchserve.pytorch.org/mar_files/densenet161.mar

        # 🚪 ingress вимикаємо (поки не треба)
        ingress:
          enabled: false

        # 📊 метрики
        metrics:
          enabled: true

        # 🛡️ ServiceMonitor для Prometheus
        serviceMonitor:
          enabled: true
          additionalLabels:
            release: prometheus-operator

        # ⚙️ ресурси (зменшені для маленьких нод)
        resources:
          limits:
            cpu: 200m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 256Mi
  syncPolicy:
    syncOptions:
      - Replace=true
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
